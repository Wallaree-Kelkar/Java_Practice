public class GarbageExample {
    /*
    Serial GC: single-threaded, stop-the-worldl event, heap division, simplest garbage collection, default 
    for small heaps
    G1 GC
    ZGC (heap > 10GB)
    */
}
